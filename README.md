# primeagents — Prime Agent Desk Team Pack

Files here are pushed into students' `my-ai-team` folder by Prime Agent Desk.
Layout mirrors the target: `.claude/skills/...`, `.claude/agents/...`, optional `CLAUDE.md`.
`manifest.json` is generated — see prime-agent-desk/docs/TEAM-PACK.md.
