---
name: {{LOOP_NAME}}
description: >
  Self-running loop: {{GOAL}}
---

# {{LOOP_NAME}}

## Goal

**Exit predicate:** {{GOAL}}

The loop runs until this condition holds. Every iteration checks the predicate
after verification passes. When it holds, the loop stops and writes a final
summary to the state file.

---

## Conventions (durable only — never write progress here)

This file is **read-only at runtime.** It carries logic, not state.

Rules:
- **Never write progress, counters, timestamps, or iteration results here.**
  All changing data belongs in the external state file at `{{STATE_PATH}}`.
- This file is loaded fresh on every cold start. Anything written here between
  runs disappears the moment the next run reloads it from disk.
- Durable knowledge only: the goal predicate, the action steps, the
  verification call, and the stop criteria.

The boundary is simple: if a value changes between runs, it goes in
`{{STATE_PATH}}`. If it never changes, it may live here.

---

## Targets (the only paths this loop may write)

The loop is allowed to create, modify, or delete **only** these paths (plus its
own `work/` scratch). Anything else is a human gate.

{{TARGET_PATHS}}

## Requirements (what must be set up before this loop can run)

The Loop Runtime checks these **before every run**. If anything is missing it
stops the loop in a **"needs setup"** state that names exactly what's absent —
instead of letting the loop start and die mid-run on an error the student can't
act on. List only what the loop genuinely needs, one per line:

- `command: <name>` — a CLI that must be installed and on PATH (e.g. `gh`, `jq`)
- `env: <VAR>` — an environment variable / API key that must be set
- `file: <path>` — an input file that must exist (relative paths resolve to the
  agent folder; `~` is the home folder)

Fill in the real requirements below, or **delete this whole section** if the loop
needs nothing beyond the vault and the student's Claude login. Do not leave a
placeholder — an empty or `{{...}}` line is skipped, and a stray one fails the lint.

```loop-requires
{{REQUIRES}}
```

---

## Verifier

Each iteration calls the verifier after acting, and only advances on exit 0:

```
{{VERIFIER_CALL}}
```

Exit 0 = this attempt passed (advance). Exit 1 = it did not (halt, surface for
review). A missing or non-executable verifier is treated as a failure.

---

## Pattern: {{PATTERN}}

The loop follows the **{{PATTERN}}** pattern. Each iteration proceeds in order:

1. **Read state** — load `{{STATE_PATH}}` to determine where the loop left off.
2. **Discover** — query the source (directory, API, queue, feed) for items to act on.
3. **Act** — perform the iteration's core action on the discovered items.
4. **Verify** — run the verifier (a separate program with a binary exit code).
   - Exit 0 → iteration passed; advance state and continue.
   - Exit 1 → iteration failed; halt and surface the anomaly for human review.
5. **Write state** — update `{{STATE_PATH}}` with the new position, counters,
   and timestamp before stopping or looping.
6. **Check exit predicate** — if `{{GOAL}}` now holds, stop; otherwise repeat.

If the verifier returns non-zero, the loop must halt immediately. Do not retry
silently. Write the failure reason to `{{STATE_PATH}}` and wait for human input
as specified in `HUMAN-GATES.md`.

---

## How to run

On Prime Agent Desk this loop runs via the desk's Loop Runtime, which reads the
`loop-trigger` block in `TRIGGER.md` and drives each iteration. To run a single
iteration by hand (or when the runtime is not enabled), open the loop in the desk
and ask the agent to *"run one iteration of the {{LOOP_NAME}} loop"* — it reads
`{{STATE_PATH}}` and this file, does one pass, runs the verifier, and updates the
state without continuing on its own. See `TRIGGER.md`.

The loop may only write to its **declared targets** (see the Targets section
below) plus its `work/` scratch. When an iteration builds a *new* artifact, stage
it in `work/` and promote it into a target only after the verifier passes — so a
failed iteration never leaves a bad partial behind. When an iteration edits an
existing target file in place, the verifier checks that target's final state
directly. Writing anywhere outside the targets is a human gate (G3).

Before the first live run:
1. Confirm `{{STATE_PATH}}` exists and is initialized (empty table, current
   timestamp in "Last run").
2. Review `HUMAN-GATES.md` — all gates listed there must be passed before
   real actions begin.
3. Confirm the verifier is executable and returns 0 on a known-good input.

---

## What "done" means

The loop is done when **all** of the following hold:

- The exit predicate `{{GOAL}}` is verifiably true.
- The verifier returned exit 0 on the final iteration.
- `{{STATE_PATH}}` has been updated with a "Last run" timestamp and a final
  status row in the ledger.
- No human gate in `HUMAN-GATES.md` is still open.

At that point the loop writes `status: done` into the ledger and exits cleanly.
